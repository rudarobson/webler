#! /usr/bin/env node

var webler = require('./webler.js');
var path = require('path');
var yargs = require('yargs')
  .usage('Usage: weble [config]')
  .help('h')
  .alias('h', 'help');

var argv = yargs.argv;

if (argv.help) {
  console.log(yargs.help());
}

var _ = argv._;
argv._ = undefined;

var f = require(path.resolve(process.cwd(), 'webler.js'));
var config = _[0];

if (!config)
  config = 'develop';

f[config].call(webler);
